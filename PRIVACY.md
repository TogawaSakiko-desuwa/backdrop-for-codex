# 隐私说明

最后更新：2026-07-26

Backdrop for Codex 是在用户计算机上运行的开源桌面伴侣。本说明描述项目自身的设计；它不覆盖 OpenAI Codex、Microsoft Store、Windows、GitHub 或其他第三方服务的数据处理。

## 核心结论

- 不包含遥测、广告、用户画像或项目自有崩溃上报。
- 不运行项目自有云服务，不代理 Codex 与 OpenAI 的通信。
- 不以功能需要为由读取、保存或上传聊天内容。
- 媒体与设置在本机处理；设置包含绝对文件路径，因此可能泄露用户名、目录结构或文件命名信息。

## 本机处理的数据

### 设置

宿主把 schema 2 设置保存在 `%LOCALAPPDATA%\CodexWallpaper\settings.json`。其中 `CodexWallpaper` 是为避免升级后丢失设置而保留的内部兼容目录名，不是当前产品展示名称。当前设置模型会保存：

- 媒体目录中的来源类型、来源标识和最近已知媒体类型；1.4.0 实际支持的本地文件来源标识是绝对路径；
- 多个方案及其 UUIDv7 标识、当前 `Global` 方案引用、稳定语义区域绑定和最近使用的最多 8 条媒体引用；
- 适配模式、裁剪焦点、深浅主题遮罩、玻璃参数、声音开关、音量和性能策略；
- 稳定语义区域与档案的绑定、CDP 风险确认，以及仅为旧设置透传的废弃 `LastCompatibilityProfileId`。

1.4.0 只实际使用本地文件、`Global` 绑定、静音播放和单个活动媒体；区域识别、Wallpaper Engine 来源和音频输出尚未实现。预留字段不会触发相应外部访问。`LastCompatibilityProfileId` 的旧字符串会保持原样、`null` 仍保持 `null`；运行时不再生成或更新该值，也不读取它作表现能力决策。

1.4.0 没有新增序列化字段，设置 schema 继续为 2。多方案、隐藏区域绑定、共享或孤立媒体引用本来就是 schema 2 的表达能力；1.4.0 的变化是由完整 V2 工作区直接维护它们。内存中的 `Draft`、`SavedDesired` 和 `ActiveSnapshot` 是同一 schema 2 文档的独立深快照，不会作为新的设置字段写入文件；运行表面的 revision、generation、lease 所有权 token、阶段和结构化错误同样是进程内状态，不会写入设置或诊断报告。

首次读取 schema 1 时，程序先把原文件的精确原始字节写入同目录的只读 `settings.v1.backup.json` 并核验一致性，再原子发布迁移后的 schema 2。备份包含旧文件原本持有的同类路径和偏好；“只读”用于防止普通误改，不是对当前用户其他进程的访问控制。损坏、不可读取、超大、迁移失败或备份冲突会进入显式恢复状态，不会以默认值静默覆盖。高于 schema 2 的文件只识别为未来版本只读状态，不会被当前版本自动保存或降级覆盖。

这些数据不会由本项目上传。能够以当前 Windows 用户身份读取设置或备份文件的其他进程，可能看到其中的路径。完整重置会删除本项目的设置和 V1 备份；仅卸载或删除程序目录不一定删除用户范围数据。显式从 V1 备份恢复会再次生成 V2，但不会改写备份本身。

### 本地媒体

程序只在用户选择后访问 PNG、JPEG、WebP、MP4 或 WebM 文件。宿主打开所选本地绝对路径，从同一文件句柄解析最终目标，确认它是受支持本地卷上的普通磁盘文件，再通过该句柄核对扩展名、文件头/容器签名和大小；图片上限为 512 MiB，视频上限为 8 GiB。网络、目录、设备和解析后落到不受支持卷的路径会被拒绝。程序在使用期间保持该只读 lease 和文件身份，因此普通编辑、替换、重命名或删除可能暂时被 Windows 拒绝，关闭或更换壁纸后句柄释放。单槽播放资源与进程内所有权 token 绑定；被取消或替代的旧 revision 只能释放自己持有的 lease，不能释放更新背景的 lease。token 本身不会序列化，也不会导出。

当前受审 Codex 页面 CSP 不允许从回环 HTTP 地址加载这些图片或视频。本项目不修改、不放宽也不绕过该 CSP。宿主经本机 CDP 把已校验、由 lease 锁定的文件绑定到页面内本项目拥有的隐藏文件输入，页面再生成 CSP 原生允许的 `blob:` URL。页面脚本在这段时间可以访问所选文件的内容，以及浏览器提供的文件名、大小、MIME type 和修改时间；它不能取得宿主传给 CDP 的完整绝对路径。宿主本身仍知道该路径，并按上一节所述保存设置。

媒体 lease 组件不运行 Kestrel、不监听临时 HTTP 端口，也不生成媒体 endpoint 或媒体访问令牌。媒体文件内容不经过 HTTP、项目自有服务或维护者控制的基础设施。详见[威胁模型](THREAT_MODEL.md)。

### CDP 与 Codex 页面

项目通过回环 CDP 给已经验证的 Codex 主工作窗口添加表现层，并设置上述隐藏文件输入。预期功能不解析或持久化聊天内容，也不从页面读取媒体的完整本机路径。页面只保留当前 `blob:` URL 及其对应的媒体节点；关闭、更换、失联或 lease 到期时会移除 `src`、撤销 `blob:` URL，并按 owner/generation 删除本项目拥有的节点和样式。旧 revision 的状态或清理事件不能操作较新的 generation。CDP 本身能够观察和控制页面，因而恶意或受损构建可能超出该用途。请验证发布来源，不要使用来源不明的二进制文件。

程序先严格验证官方包、Publisher、AppId、进程、当前会话、监听器、严格 IPv4 回环端点、CDP browser/socket/target 和唯一工作页；失败时不会执行结构证据探针。通过后，程序只把页面结构归纳为匹配 `global-baseline-v1` 与 `codex-shell-v1` 所需的布尔证据。Codex 版本只用于身份自洽和下述诊断，原始 DOM、页面 URL 与受审核选择器不会被保存或导出。

### 日志和诊断信息

程序可能显示或生成本地运行状态、版本、连接阶段和结构化错误信息，以便排错。界面会区分草稿未保存、已保存未激活、媒体运行中、官方背景、激活失败和 revision 被替代；这些标签不是遥测，也不会被项目上传。项目自有的用户消息和日志设计上不记录聊天正文或媒体绝对路径。底层系统异常的内部对象仍可能包含文件路径，因此不应公开上传异常转储。

“设置 → 关于”中的诊断导出只在用户明确操作、确认说明并选择保存位置后创建本地 JSON 文件；程序不会在后台生成、定期收集或自动上传它。`schemaVersion: 2` 报告固定分为 Environment、Runtime 和 Compatibility，只包含：

- 报告 schema 版本；
- 应用版本、Windows 版本、进程架构和 .NET 运行时描述；
- 壁纸运行阶段、是否活动和是否暂停；
- Codex 四段版本、类型化安全阶段与终态原因；
- 活动结构契约、匹配状态，以及五类兼容能力的枚举状态与枚举原因。

报告结构没有媒体路径或文件名、包完整名、Publisher、进程/PID/会话/端口、页面标题、完整 URL、DOM、选择器、聊天、设置内容、CDP Detail、用户/设备标识符、散列或原始异常字段。它也不是日志、设置或内存转储的打包器。用户选择的保存目录仍可能由 OneDrive、企业备份或其他第三方软件同步，分享前应检查实际文件。

错误、操作系统或第三方库仍可能产生未预见的信息。分享诊断报告以外的日志、截图、设置文件或转储前，请检查并删除用户名、绝对路径、聊天、令牌和其他敏感内容。内存转储可能包含完整进程内存，不应公开上传。

## 网络通信

项目自身正常运行所需的网络控制连接仅限本机严格 IPv4 回环 CDP。媒体内容通过页面内 `blob:` URL 读取，不经 HTTP 或外部网络；伴侣不为媒体启动任何网络监听器。退出伴侣会释放 lease，但 Codex 自身持有的 CDP 端口会持续到 Codex 完全退出。

Codex 桌面应用会独立连接 OpenAI 服务，Windows、Microsoft Store、GitHub CLI 或操作系统组件也可能按各自设置联网。这些通信不是由本项目代理或控制，适用相应提供方的政策。

## 保留、删除和备份

本项目维护者不会收到上述本地数据，因此无法代用户访问、导出或删除。“恢复官方背景”会取消未完成的应用并移除媒体 `src`、撤销当次 `blob:` URL、删除自有页面节点和释放本应用持有的 lease，但不会修改 `SavedDesired`；空方案则会作为正常 schema 2 目标保存。设置和 V1 备份会保留，直到用户执行完整重置或自行删除对应文件。真正退出、完整重置或恢复 V1 备份遇到脏草稿时，只允许用户丢弃后继续或取消，不会隐式保存/应用。用户导出的诊断报告保存在用户选择的位置，不由重置功能代为搜索或删除。系统备份、企业漫游配置、取证工具或同步软件可能另行保留副本，需由用户或管理员管理。

首次成功增强启动后，程序会在当前用户桌面创建或更新 `Codex（动态背景）.lnk`，其参数为 `--launch`。删除该快捷方式可移除入口；取消设置窗口中的风险确认会阻止媒体方案下次自动增强启动。空方案通过该入口启动时只验证并普通启动官方 Codex，不附加调试参数，也不建立 CDP。快捷方式不属于系统级启动项，也不会让程序随 Windows 自动启动。若伴侣已在托盘，第二实例只通过当前用户、当前 Windows 会话的本地命名管道转发显示或启动命令。

## 儿童、出售和共享

本项目不面向儿童收集数据，不出售个人信息，也不与维护者控制的广告商或数据经纪商共享数据，因为项目没有收集管道。

## 变更

如果未来功能需要遥测、远程服务或新增数据类别，必须先公开更新本说明与威胁模型，并提供明确、可撤回的选择；不得在普通维护更新中静默加入。版本历史可通过 Git 查看。

隐私或安全问题请按 [SECURITY.md](SECURITY.md) 私下报告。
